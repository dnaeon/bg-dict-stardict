name: Verify Bulgarian StarDict coverage

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/**'
      - 'Makefile'
      - 'vendor/**'
      - '.github/workflows/verify.yml'
  pull_request:
    paths:
      - 'scripts/**'
      - 'Makefile'
      - 'vendor/**'
      - '.github/workflows/verify.yml'

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            make zip xz-utils \
            cmake g++ pkg-config libglib2.0-dev zlib1g-dev gettext

      - name: Build and install sdcv from source
        # Ubuntu noble ships sdcv 0.5.2 (Aug 2017) which fails to handle
        # Cyrillic input on this runner. We build the upstream pinned tag
        # instead. See scripts/install-sdcv.sh for the rationale.
        run: ./scripts/install-sdcv.sh

      - name: Install Python dependencies
        run: pip install --upgrade pyglossary python-idzip

      - name: Run unit tests
        run: make test

      - name: Build the dictionary
        run: make build

      - name: Exhaustively verify coverage
        run: make verify

      - name: Coverage report
        if: always()
        run: |
          {
            echo "## Verification result"
            echo ""
            if [ -f out/stardict/bulgarian/bulgarian.ifo ]; then
              echo '```'
              grep -E '^(wordcount|synwordcount|bookname)=' out/stardict/bulgarian/bulgarian.ifo
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"
